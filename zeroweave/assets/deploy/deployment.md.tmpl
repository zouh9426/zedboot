# 部署说明：<项目名>

> 由 ZeroWeave skill 生成。本文件入库，**只写占位符**；服务器 IP、SSH 端口、密钥真实路径、crontab 具体调度等不可推导运维值统一存本地 `docs/private/ops.md`（已 gitignore，永不入库）。

## 1. 信息登记（位置与引用）

| 项 | 值 |
|---|---|
| 服务器 IP | `<PRODUCTION_SERVER_IP>`（真实值见 docs/private/ops.md） |
| SSH 端口 | `<SSH_PORT>`（真实值见 docs/private/ops.md） |
| 项目账号 | <项目名>（专用账号，默认 = 项目名） |
| 部署密钥（服务器端） | `/home/<项目名>/.ssh/<项目名>_deploy` |
| 部署密钥（本地路径） | `~/.ssh/<项目名>_deploy`（默认约定；偏离时真实路径见 docs/private/ops.md） |
| GitHub 仓库 | <仓库地址>（私有，仅备份与版本管理） |
| 域名 | <域名> |
| DNS 托管商 | <DNS托管商> |
| 容器端口（本机回环） | `127.0.0.1:<端口>` |
| 备份位置 | `/opt/<项目名>/backups/`（crontab 具体调度见 docs/private/ops.md） |
| 备份保留份数 | 7（滚动，可按需调整） |
| 数据目录 | `/opt/<项目名>/data` |

> 秘密本体（私钥内容、密码、token）**永不写入本文件与 Git**，只存在于服务器 `.env` 与本地。

## 2. 日常运维速查

- **发布（一行命令链）**

  ```bash
  # 本地：合回 main 后先 push 备份，再 rsync 直推
  # <PRODUCTION_SERVER_IP> / <DEPLOY_USER> 真实值见本地 docs/private/ops.md（不入库）
  git push && rsync -az --delete --exclude .git --exclude node_modules --exclude .env \
    --exclude data --exclude backups -e "ssh -i ~/.ssh/<DEPLOY_KEY>" \
    ./ <DEPLOY_USER>@<PRODUCTION_SERVER_IP>:/opt/<项目名>/
  # 服务器：重建并自动迁移
  cd /opt/<项目名> && docker compose up -d --build
  ```

- **看日志**：`docker compose logs -f <项目名>`
- **重启**：`docker compose restart <项目名>`
- **备份**：crontab 自动执行 `backup.sh`，备份在 `/opt/<项目名>/backups/`，滚动保留 N 份（真实调度见 docs/private/ops.md）
- **恢复**：停容器 → 覆盖数据目录（或恢复 SQLite 一致副本）→ 重启

## 3. 原理参考

完整规范见 ZeroWeave skill 的 `references/deployment-guide.md`；本文件由 AI 依据该规范生成。
