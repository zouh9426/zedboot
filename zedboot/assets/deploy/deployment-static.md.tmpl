# 静态站部署说明：<项目名>

> 由 zedboot skill 生成。本文件入库，**只写占位符**。
> 部署五事实（项目名/账号/目录/IP/密钥）统一存本地 `docs/private/deploy.env`（已 gitignore，永不入库）；
> 其余不可推导运维值（SSH 端口、crontab 调度等）存本地 `docs/private/ops.md`。
> 本项目为静态站（无容器）：本地构建产物由服务器共享 Caddy 直接文件伺服，无服务进程、无数据库、无容器，不依赖 Docker。

## 1. 信息登记（位置与引用）

| 项 | 值 |
|---|---|
| 服务器 IP | `SERVER_IP`（真实值见 docs/private/deploy.env） |
| SSH 端口 | `<SSH_PORT>`（真实值见 docs/private/ops.md） |
| 项目账号 | `DEPLOY_USER`（真实值见 docs/private/deploy.env） |
| 部署密钥（服务器端） | `/home/<服务器账号>/.ssh/<项目名>_deploy` |
| 部署密钥（本地路径） | `DEPLOY_KEY`（真实值见 docs/private/deploy.env） |
| GitHub 仓库 | <仓库地址>（私有，仅备份与版本管理） |
| 域名 | <域名> |
| DNS 托管商 | <DNS托管商> |
| 静态站目录（服务器） | `REMOTE_DIR`，如 `/opt/<项目名>/dist`（发布目录，共享 Caddy 文件伺服） |
| 发布目录（本地） | `STATIC_OUTPUT_DIR`：Vite/Astro=`dist`、Next.js 静态导出=`out`、纯 HTML=`public` |
| 部署脚本（本地） | `deploy-rsync-static.sh`（只推发布目录） |
| 备份 | 无应用数据；备份 = 仓库本身 + 服务器发布目录（异地容灾见 docs/private/ops.md） |

> 秘密本体（私钥内容、密码、token）**永不写入本文件与 Git**，只存在于服务器与本地。
> 部署事实由 `docs/private/deploy.env` 提供（模板见 zedboot `assets/project/deploy.env.tmpl`）：
> 三事实分离——本地目录名 ≠ 项目名 ≠ 服务器账号，脚本不从路径推导部署事实；
> 发布目录由 `STATIC_OUTPUT_DIR` 控制，找不到即报错退出（fail-closed，绝不发布项目根；含 `STATIC_OUTPUT_DIR=.` 或越出项目的路径会被拒绝）。

## 2. 日常运维速查

- **发布（一行命令链）**

  ```bash
  # 本地：构建后执行部署脚本，只推发布目录到服务器
  # 部署事实来自 docs/private/deploy.env（不入库），无需传环境变量
  npm run build && ./deploy-rsync-static.sh
  # 服务器：无需任何操作——共享 Caddy 直接伺服发布目录，rsync 完成即生效
  ```

- **生效**：`file_server` 按请求读盘，rsync 完成即生效，无需重启、无需 reload；仅改动服务器 Caddyfile 后才需 `caddy reload`
- **看日志**：无容器、无应用进程日志；Caddy 访问/错误日志在服务器（真实位置见 docs/private/ops.md）
- **重启**：无容器、无服务进程，无需重启；改完发布目录重新 rsync 即更新
- **备份**：静态站无应用数据，备份 = 仓库本身（已 push 私有 GitHub 仓库）+ 服务器发布目录；无 backup.sh 数据备份，建议将发布目录同步到对象存储做异地容灾（调度见 docs/private/ops.md）
- **恢复**：仓库 clone + `npm run build` + 重新 rsync；或从服务器发布目录备份直接恢复

## 3. 原理参考

完整规范见 zedboot skill 的 `references/deployment-guide.md` 与 `assets/deploy/static/README.md`；本文件由 AI 依据该规范生成。
