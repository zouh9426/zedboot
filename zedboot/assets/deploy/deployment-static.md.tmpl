# 静态站部署说明：<项目名>

> 由 zedboot skill 生成。本文件入库，**只写占位符**；服务器 IP、SSH 端口、密钥真实路径、crontab 具体调度等不可推导运维值统一存本地 `docs/private/ops.md`（已 gitignore，永不入库）。
> 本项目为静态站（无容器）：本地构建产物 `dist/` 由服务器共享 Caddy 直接文件伺服，无服务进程、无数据库、无容器，不依赖 Docker。

## 1. 信息登记（位置与引用）

| 项 | 值 |
|---|---|
| 服务器 IP | `<PRODUCTION_SERVER_IP>`（真实值见 docs/private/ops.md） |
| SSH 端口 | `<SSH_PORT>`（真实值见 docs/private/ops.md） |
| 项目账号 | <项目名>（专用账号，默认 = 项目名） |
| 部署密钥（服务器端） | `/home/<项目名>/.ssh/<项目名>_deploy` |
| 部署密钥（本地路径） | `~/.ssh/<项目名>_deploy`（默认约定；偏离时真实路径见 docs/private/ops.md） |
| GitHub 仓库 | <仓库地址>（私有，仅备份与版本管理） |
| 域名 | <域名> |
| DNS 托管商 | <DNS托管商> |
| 静态站目录（服务器） | `/opt/<项目名>/dist`（构建产物，共享 Caddy 文件伺服） |
| 部署脚本（本地） | `deploy-rsync-static.sh`（只推 `dist/`） |
| 备份 | 无应用数据；备份 = 仓库本身 + 服务器 `/opt/<项目名>/dist`（异地容灾见 docs/private/ops.md） |

> 秘密本体（私钥内容、密码、token）**永不写入本文件与 Git**，只存在于服务器与本地。

## 2. 日常运维速查

- **发布（一行命令链）**

  ```bash
  # 本地：构建后执行部署脚本，只推 dist/ 到服务器
  # <PRODUCTION_SERVER_IP> 真实值见本地 docs/private/ops.md（不入库）；部署账号 = 项目名，可推导
  npm run build && PRODUCTION_SERVER_IP=<PRODUCTION_SERVER_IP> ./deploy-rsync-static.sh
  # 服务器：无需任何操作——共享 Caddy 直接伺服 /opt/<项目名>/dist，rsync 完成即生效
  ```

- **生效**：`file_server` 按请求读盘，rsync 完成即生效，无需重启、无需 reload；仅改动服务器 Caddyfile 后才需 `caddy reload`
- **看日志**：无容器、无应用进程日志；Caddy 访问/错误日志在服务器（真实位置见 docs/private/ops.md）
- **重启**：无容器、无服务进程，无需重启；改完 `dist/` 重新 rsync 即更新
- **备份**：静态站无应用数据，备份 = 仓库本身（已 push 私有 GitHub 仓库）+ 服务器 `/opt/<项目名>/dist` 目录；无 backup.sh 数据备份，建议将 dist 同步到对象存储做异地容灾（调度见 docs/private/ops.md）
- **恢复**：仓库 clone + `npm run build` + 重新 rsync；或从服务器 dist 备份直接恢复

## 3. 原理参考

完整规范见 zedboot skill 的 `references/deployment-guide.md` 与 `assets/deploy/static/README.md`；本文件由 AI 依据该规范生成。
