# ============================================================
# .dockerignore 模板（zedboot skill）
# 落地：复制到项目根目录为 .dockerignore
# 为什么必须有：模板 Dockerfile 的构建阶段使用 `COPY . .`，
# 没有本文件会把 .env（密钥）、data/（线上数据）、.git 等拷进镜像层——
# 密钥随镜像泄露是最大的风险项。
# ============================================================

# 版本控制与依赖（构建上下文不需要）
.git
node_modules

# 敏感与运行时（永不进镜像）
.env*
!.env.example
!.env.sample
!.env.template
data
backups

# 文档与项目管理（镜像不需要）
docs
archive
*.md
!README.md

# 系统与杂项
.DS_Store
*.log
