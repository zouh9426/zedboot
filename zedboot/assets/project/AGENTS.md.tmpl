# AGENTS.md

## 项目识别

- 项目名称：
- 项目代码：
- 项目模式：
- 当前正式分支：
- 管理规范版本：v1.4
- UI 规范：DESIGN.md（如项目有界面；它是唯一 UI 规范来源，任何 UI 变更走 zedui 工作流，不绕过）
- 部署文档：docs/guides/deployment.md（如为可部署项目；含服务器/域名/端口等登记信息）
- 完整参考版位置：<随项目安装的路径，或注明"未随项目安装，见 zedboot skill 的 references/ 目录">

## 阅读纪律（分层加载）

**每次任务必读**（均为小文件，合计约 6 KB）：

1. AGENTS.md
2. docs/project/PROJECT_STATE.md
3. docs/project/TODO.md
4. 当前任务引用文件

**按触发读**（用到才读，只读相关小节）：

- `docs/project/PROJECT_RULES.md`：流程裁决、文档同步、交接，以及收尾自检第 5 维「同步完整」执行时（更新触发矩阵在其中）
- `README.md`：首次接触本项目或交接时
- `docs/project/PROJECT_INDEX.md`：需要定位架构、资源、外部依赖信息时
- `docs/project/DECISION_LOG.md`：做重大决策或需要历史裁决依据时
- `docs/guides/deployment.md`：部署相关任务时（如为可部署项目）
- `DESIGN.md`：UI 相关任务时（如项目有界面）

## 项目特有约束

- 运维/部署真实值（服务器 IP、SSH 端口、密钥路径、crontab 调度等）统一存本地 `docs/private/`（已 gitignore，永不入库）；入库文档只写占位符（如 `<PRODUCTION_SERVER_IP>`、`<DEPLOY_USER>`）+ 指向 `docs/private/ops.md` 的注记。
- 禁止将密钥、密码、token、服务器 IP、本机绝对路径写入入库文件。
- 

## 默认跳过

- archive/
- 依赖目录
- 构建产物
- 缓存
- 日志
- 大型二进制文件
- .env 与任何密钥文件（永不读取内容，永不入库）

## 当前写入规则

- 同一时间只有一个当前写入者。
- 未确认写入权时只做分析。
- AI 只可将任务推进到待验收。

## 每任务收尾自检（进入待验收前必做，自动执行，不等用户指示）

**位阶**：本清单是**最低必查，不是检查上限**——任务现场提示的风险点优先查（你最懂这个任务），清单只兜住没想到的。发现新的漏检类型时，把对应机械查法补进本清单；长期未命中的条目可降级或删除。

**机械三查**（复制即跑，前两条无输出才算过）：

```bash
git ls-files -co --exclude-standard | xargs grep -nE '/Users/[a-zA-Z0-9_-]+/'
git ls-files -co --exclude-standard | xargs grep -nE 'BEGIN [A-Z ]*PRIVATE KEY'
git status --short
```

> 扫描范围 = git candidate 文件（已跟踪 + 未跟踪且未被 `.gitignore` 排除，与 verify.py 口径一致）；`.env`、`docs/private/` 等已被 `.gitignore` 排除——既不会被读取也不需要扫描。前两条有输出就不许 commit。

**五个维度与处置**：

1. **安全红线**：机械三查前两条无命中。命中 → 改：替换为占位符，真实值挪 `docs/private/ops.md`。
2. **一致性冲突**：本次改动涉及的步骤编号 / 术语 / 文件路径，反向 grep 全仓库确认其他引用处仍成立；新旧指令无矛盾。冲突 → 改：对齐到最新事实。
3. **失效残留**：`git status` 里本次新增文件逐一审视——产物类（测试缓存、临时输出）直接删；git 跟踪文件双命中（全仓库零引用 + 产物/过期特征）可删；拿不准一律不删，登记 TODO。
4. **功能**：本次改动波及的测试、构建或内容检查通过；脚本过 `bash -n`（或对应语法检查）。坏 → 改：直接修，修不了在报告中列出。
5. **同步完整**：TODO、PROJECT_STATE、PROJECT_INDEX、CHANGELOG 按 PROJECT_RULES 触发矩阵同步；被替代的旧文件已删除或归档。

**闭环**：所有修复完成后，机械三查复跑一遍 + 纵览本次完整 diff——修复本身可能引入新错。

**报告**：交付时附一行自检结论，按维度给结果，例：`自检：红线干净｜冲突 1 处已修｜删 2 个测试临时文件｜测试通过｜CHANGELOG 已同步`。需要用户裁决的单独列出。

**发版本前加做（全版）**：五维全量（含死文件候选排查：零引用 grep + `git log -1` 长期未动文件）；清理后复跑项目既有验证（测试/构建/部署检查）；轻版复跑。

## 文档同步

遵循 PROJECT_RULES.md 的更新触发矩阵。

## 交接

交接格式见 PROJECT_RULES.md，不在本文件重复定义。
