# 运维真实值（本地私有，永不入库）

> 本文件由 `.gitignore` 的 `docs/private/` 规则保护，不会被提交或推送。
> 入库文档中的 `<PRODUCTION_SERVER_IP>`、`<DEPLOY_USER>`、`~/.ssh/<DEPLOY_KEY>` 等占位符的真实值在此查阅。
> 本文件不进 Git、没有版本备份——请为它配置独立私有备份通道（如私有 ops-notes 仓库），否则换机即丢。
> 建立：<日期>（zedboot 开局安装）

## 机器可读字段（人读参考；机器读以 `docs/private/deploy.env` 为准——pre-push/audit 对旧项目保留本段 ops.md fallback；保持"键: 值"单行格式）

> 机器可读约定：仓库目录名、项目名、服务器账号是**三个独立事实**，不得混为一谈
> （目录取英文缩写、项目名用中文、账号另有约定——三者互不相同很常见）。新项目的机器
> 读部署事实（部署六事实）统一由 `docs/private/deploy.env` 提供，本文件为**人读参考**；
> 旧项目（未建 deploy.env）pre-push 隐私闸门与 audit.py 仍读取本段「机器可读字段」
> （服务器账号只登记本处一次，运行时各自读取；安装闸门时无需为账号做任何配置）。
> 格式：单行、"键: 值"（键后中/英文冒号均可、值不含空格）。

- 服务器账号: <DEPLOY_USER>

## 服务器

- 服务商/机型：
- IP：
- SSH 端口：
- 专用账号：
- 本机 SSH 别名（~/.ssh/config，如有）：
- 部署密钥（本地私钥真实路径）：
- 应用目录：/opt/<项目名>（数据 /opt/<项目名>/data）

## 部署流程

- 发布命令链 / 触发方式：
- 容器端口（本机回环）：

## 备份

- crontab 具体调度（真实时间与日志路径）：
- 保留份数：
- 异地容灾（如有）：

## 联系信息

- 邮箱（如为公开联系方式，入库文档保留属有意决定）：
